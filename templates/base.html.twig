<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>

        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        
        <!-- Font Awesome CSS -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        
        <!-- CSS personalizado -->
        <link rel="stylesheet" href="/css/app.css">

        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <div class="alert alert-warning text-center" role="alert">
            ⚠ Recuerda que esta aplicación es una prueba técnica. Es probable que las funcionalidades sean bastante básicas, ¡pero aún así tiene su encanto! 🤭.
        </div>

        <div class="row float-end p-3">
            <div class="col">
                <button onclick="history.back()" class="btn btn-outline-secondary"><i class="fa-solid fa-rotate-left"></i> Atrás</button>
            </div>
        </div>

        {% for flash_message in app.flashes('success') %}
            <div class="alert alert-success text-center">
                {{ flash_message }}
            </div>
        {% endfor %}
        {% for flash_message in app.flashes('message') %}
            <div class="alert alert-primary text-center">
                {{ flash_message }}
            </div>
        {% endfor %}
                {% for flash_message in app.flashes('warning') %}
            <div class="alert alert-warning text-center">
                {{ flash_message }}
            </div>
        {% endfor %}

        <div class="container-fluid px-5">
            {% block body %}{% endblock %}
        </div>

        <!-- Bootstrap JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    </body>
</html>

