{% extends 'base.html.twig' %}

{% block title %}Hello ProviderController!{% endblock %}

{% block body %}
<style>
</style>

<div class="container-fluid px-5">

    <section id="header">
        <div class="row d-flex justify-content-center" style="padding-top: 48px;">
            <div class="col-6 text-center">
                <h3 style="font-size: 1.75rem; font-weight: bold;">
                    Listado Proveedores
                </h3>
                <p>
                    Localiza usuarias, edita sus datos, elíminalas, exporta los datos filtrados...
                    <br>
                    ⚠️ No olvides que el panel de administración está en construcción y seguramente habrá cosas que no funcionen 🦺.
                </p>
            </div>
            <div class="col-12 d-flex justify-content-center">
                <a href="#" class="btn btn-primary btn"><i class="fas fa-plus"></i> Crear Usuario</a>
            </div>
        </div>
    </section>

    <section id="cuerpo-proveedores">
        <div class="card">
            <div class="card-header">
                ...
            </div>
            
            <div class="card-body">
                @if (@empty(usuarios))
                <div class="alert alert-warning">
                    🤷🏻‍♀️ No hay usuarios...
                </div>
                @else
                <table class="table">
                    <thead>
                        <tr>
                            <th>Id <button class="btn-sm">↕</button></th>
                            <th>Nombre <button class="btn-sm">↕</button></th>
                            <th> __('Apellidos') </th>
                            <th>Nick (@)</th>
                            <th>Email</th>
                            <th>Teléfono</th>
                            <th>Alta registro <span title="Fecha de la verificación del email">ℹ️</span></th>
                            <th> __('Acciones') </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (usuarios as usuario)
                        <tr>
                            <td> usuario->id </td>
                            <td> usuario->nombre </td>
                            <td> usuario->apellidos </td>
                            <td> usuario->nick </td>
                            <td> usuario->email </td>
                            <td> usuario->telefono </td>
                            <td>
                                @if (usuario->email_verified_at)
                                     usuario->email_verified_at->format('d-m-Y') 
                                @else
                                    No verificado
                                @endif
                            </td>
                            <td class="d-flex justify-content-start">
                                <a href="#" class="btn btn-outline-primary" role="button" title="Ver datos de la persona"><i class="far fa-eye"></i></a>
                                <a href="#" class="btn btn-outline-primary" role="button" title="Editar datos de la persona"><i class="fas fa-edit"></i></a>
                                <form action="#" method="POST">
                                    <button type="submit" class="btn btn-outline-primary" title="Borrar la persona"><i class="far fa-trash-alt"></i></button>
                                </form>
                                <a href="#" class="btn btn-outline-primary" role="button" title="Enviar reestablecimiento de contraseña"><i class="fa-regular fa-paper-plane"></i></a>
                            </td>
                        </tr>                    
                        @endforeach
                    </tbody>
                </table>

                <!--AÑADO LA PAGINACION-->
                <div class="clearfix"></div>
                <!-- ... -->
                @endif           
            </div>
        </div>
    </section>

</div>
{% endblock %}
